---
title: A quick thought
date: 2026-04-14
summary: Notes use `summary` instead of `description` and `categories` instead of `tags`.
categories:
  - aside
  - meta
---

Notes are short. The interesting bit is that this collection's frontmatter
uses different field names from `posts` — exercising the per-collection
resolver remap declared in `astro.config.ts`.
