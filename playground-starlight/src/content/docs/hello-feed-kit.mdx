---
title: Hello, feed-kit
description: First demo entry — exercises the default resolver chain and MDX renderer.
date: 2026-04-10
---

This is a short MDX entry used to exercise `astro-feed-kit`'s default resolver
chain inside a Starlight docs collection. Its rendered body should appear
inside `<content:encoded>` in the RSS output.

## Headings render through

A second-level heading and some **bold** text confirm the markdown renderer
path runs end-to-end before the feed is serialized.
